(function () {
    'use strict';

    angular.module('smartme.passwordsafe', ['ngResource', 'ngRoute']);

    angular.module('smartme.passwordsafe').config(config);

    function config($routeProvider) {
        /*
        $routeProvider
            .when('/passwordsafe', {
                templateUrl: 'modules/passwordsafe/passwordsafe.html',
                controller: 'PasswordSafeController'
            });
        */
    }


})();